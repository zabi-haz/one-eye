import { Alert, Button, StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import React,{ useState } from "react"

const App : React.FC = () =>{
  return(
    <View>
      <Text>Welcom to life-env</Text>
    </View>  
  )
}
const styles = StyleSheet.create({

});
export default App;
